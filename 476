# part 1
from collections import defaultdict

def word_to_pattern(word):
    word = word.lower()
    mapping = {}
    pattern = ''
    current_char = ord('a')
    for char in word:
        if char not in mapping:
            mapping[char] = chr(current_char)
            current_char += 1
        pattern += mapping[char]
    return pattern
